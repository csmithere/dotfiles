return {
  'echasnovski/mini.align',
  version = '*', -- Use latest stable version
  config = function()
    -- You can add custom setup here if needed, but defaults are great
    require('mini.align').setup()
  end,
}
